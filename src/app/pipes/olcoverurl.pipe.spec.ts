import { OlcoverurlPipe } from './olcoverurl.pipe';

describe('OlcoverurlPipe', () => {
  it('create an instance', () => {
    const pipe = new OlcoverurlPipe();
    expect(pipe).toBeTruthy();
  });
});
